解题思路：
区间型DP，特殊点在于需要将选定的 气球 后打，如果先打的话，需要不断地更新 board 来确定选定气球的左右邻居。
如果后打，i-1 [i, k - 1] k [k + 1, j] j+1  假设选定的气球为k，那么在[i, k - 1] [k + 1, j]这两个区间已经被打完。
k 的左右邻居渐变成 i-1 和 j+1。这样就变为先对固定的情况，不需要一直更新board，减少cost

区间型dp:
for (int len = 1; len <= inputLen; len++) { // 区间的长度
    for (int i = 1; i + len - 1 <= inputLen; i++) { // 区间的开始位置
        int j = i + len - 1; // 区间的end
        for (int k = i, k <= j; k++ { // 选择的k
            ... // 本题注意的是选择的k需要最后处理
        }
    }
}