解题思路：
首先需要分析题目的分叉思路，由题可知，只有在碰到"a*"结构的时候才会有分叉，"a*"可以代表 空, a, aa, aaa, ...
所以着重分析 a* 结构的写法，注意dfs函数的定义，以及string s 第二个指针的设计技巧。
时间复杂度：需要填一个 m = s.length(), n = p.length(), m * n的表格，填写每个格子的cost是 n。所以时间复杂度是O(m * n^2)